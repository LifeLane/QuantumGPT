{
  "build": {
    "env": {
      // Define build-time environment variables here if needed.
      // Example: "NEXT_PUBLIC_VERCEL_ENV": "production"
    }
  },
  "env": {
    // Define runtime environment variables here.
    // These are typically sensitive variables that should NOT be in your code.
    // It is recommended to configure these directly in the Vercel dashboard
    // using "Environment Variables" settings.
    // Example: "DATABASE_URL": "@my_database_url_secret" // Referencing a Vercel Secret
  },
  // Optional: Add framework for explicit declaration
  "framework": "nextjs"
}